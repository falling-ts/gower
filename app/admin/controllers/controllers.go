package controllers

import "gitee.com/falling-ts/gower/app"

var (
	exc    = app.Exception()
	passwd = app.Passwd()
	res    = app.Response()
	config = app.Config()
	auth   = app.Auth()
	cookie = app.Cookie()
	db     = app.DB()
)
